name: CI for CeilingsApp

on:
  push:
    branches:
      - dev
      - main
  pull_request:
    branches:
      - dev
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Шаг 1: Клонируем репозиторий
      - name: Checkout code
        uses: actions/checkout@v3

      # Шаг 2: Устанавливаем Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # Шаг 3: Кэшируем зависимости
      - name: Cache node_modules
        uses: actions/cache@v3
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      # Шаг 4: Очистка кэша npm
      - name: Clear npm cache
        run: npm cache clean --force

      # Шаг 5: Устанавливаем зависимости и EAS CLI
      - name: Install dependencies and EAS CLI
        run: |
          npm install -g eas-cli
          npm ci

      # Шаг 6: Проверяем переменные окружения
      - name: Print EXPO_USERNAME and EXPO_PASSWORD
        run: |
          echo "EXPO_USERNAME is: $EXPO_USERNAME"
          echo "EXPO_PASSWORD is: $EXPO_PASSWORD"

      # Шаг 7: Логинимся в Expo с использованием имени пользователя и пароля
      - name: Log in to Expo
        env:
          EXPO_USERNAME: ${{ secrets.EXPO_USERNAME }}  # Секрет с именем пользователя
          EXPO_PASSWORD: ${{ secrets.EXPO_PASSWORD }}  # Секрет с паролем
        run: |
          echo "Logging in with Expo username and password..."
          eas login --username $EXPO_USERNAME --password $EXPO_PASSWORD  # Логин с использованием имени и пароля

      # Шаг 8: Сборка проекта для Android
      - name: Build project (Android)
        run: |
          echo "Starting build..."
          eas build -p android --non-interactive || echo "Build failed"
